import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.btsbots.market',
  appName: 'btsbots-market',
  webDir: 'dist-market'
};

export default config;
