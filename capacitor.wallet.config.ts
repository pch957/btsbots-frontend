import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.btsbots.wallet',
  appName: 'btsbots-wallet',
  webDir: 'dist-wallet'
};

export default config;
